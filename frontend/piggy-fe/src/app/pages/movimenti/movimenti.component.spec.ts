import { MovimentiComponent } from './movimenti.component';

describe('MovimentiComponent', () => {
  it('defines the default search mode as latest movements', () => {
    expect(MovimentiComponent).toBeTruthy();
  });
});
